function initTabs() { document.querySelectorAll('.code-tabs').forEach(tabGroup => { const tabs = tabGroup.querySelectorAll('.code-tab'); const container = tabGroup.parentElement; const panels = container.querySelectorAll('.code-panel'); tabs.forEach(tab => { tab.addEventListener('click', () => { tabs.forEach(t => t.classList.remove('active')); panels.forEach(p => p.classList.remove('active')); tab.classList.add('active'); const lang = tab.dataset.lang; container.querySelector(`.code-panel.${lang}`).classList.add('active') }) }) }); document.querySelectorAll('.code-tabs .code-tab:first-child').forEach(t => { t.click() }) }
document.addEventListener('DOMContentLoaded', initTabs);
